[theme]
# Base claire stable. Le mode sombre est proposé par un interrupteur dans l'app
# (habillage CSS injecté), indépendant du thème natif de Streamlit.
base = "light"
primaryColor = "#0E7A63"
font = "sans serif"
