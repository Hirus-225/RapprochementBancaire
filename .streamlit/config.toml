[theme]
# Pas de `base` figé : l'application suit le thème clair/sombre du système de
# l'utilisateur, et Streamlit re-thème alors tous les composants (tableaux
# compris). Seuls l'accent et la police sont imposés — ils tiennent sur les deux.
primaryColor = "#0E7A63"
font = "sans serif"
